name: perizinan_santri
description: Aplikasi Perizinan Pulang Kembali Santri

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase Core (WAJIB)
  firebase_core: ^4.3.0
  
  # Firebase Authentication (untuk login)
  firebase_auth: ^6.1.3
  
  # Cloud Firestore (database)
  cloud_firestore: ^6.1.1
  
  # State Management
  provider: ^6.1.1
  
  # UI
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
